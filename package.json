{
  "name": "docker-wp",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "browser-sync start --config bs-config.js --no-ui --no-online & docker-compose up",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.1"
  }
}